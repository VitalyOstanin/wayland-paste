# Packaged extension archives
*.zip

# Editor / OS noise
.DS_Store
*.swp
*~

# Compiled translations (keep .po, ignore generated .mo if added later)
*.mo

# Compiled GSettings schema (generated by glib-compile-schemas)
schemas/gschemas.compiled

# project-check reports (local only)
docs/reviews/

# Persisted clipboard history written at runtime, if a copy lands in the tree
history.json
images/

# dev-reload.sh state
tools/.dev-state
